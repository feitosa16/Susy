#include <stdio.h>
int main() {
        int d, m, a;
        printf(d"/"m"/"a);
        scanf("%d,%d,%d", &d, &m, &a);
        if (m>12) {
            printf("Mes invalido\n")
    }
return 0;
}